import { GetRarityImagePipe } from './get-rarity-image.pipe';

describe('GetRarityImagePipe', () => {
  it('create an instance', () => {
    const pipe = new GetRarityImagePipe();
    expect(pipe).toBeTruthy();
  });
});
