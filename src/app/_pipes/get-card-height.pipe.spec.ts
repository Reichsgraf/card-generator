import { GetCardHeightPipe } from './get-card-height.pipe';

describe('GetCardHeightPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCardHeightPipe();
    expect(pipe).toBeTruthy();
  });
});
