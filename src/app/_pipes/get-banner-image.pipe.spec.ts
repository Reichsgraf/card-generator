import { GetBannerImagePipe } from './get-banner-image.pipe';

describe('GetBannerImagePipe', () => {
  it('create an instance', () => {
    const pipe = new GetBannerImagePipe();
    expect(pipe).toBeTruthy();
  });
});
