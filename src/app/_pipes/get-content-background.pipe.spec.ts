import { GetContentBackgroundPipe } from './get-content-background.pipe';

describe('GetContentBackgroundPipe', () => {
  it('create an instance', () => {
    const pipe = new GetContentBackgroundPipe();
    expect(pipe).toBeTruthy();
  });
});
