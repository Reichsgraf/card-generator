import { GetFrameImagePipe } from './get-frame-image.pipe';

describe('GetFrameImagePipe', () => {
  it('create an instance', () => {
    const pipe = new GetFrameImagePipe();
    expect(pipe).toBeTruthy();
  });
});
