import { GetProvisionFontSizePipe } from './get-provision-font-size.pipe';

describe('GetProvisionFontSizePipe', () => {
  it('create an instance', () => {
    const pipe = new GetProvisionFontSizePipe();
    expect(pipe).toBeTruthy();
  });
});
