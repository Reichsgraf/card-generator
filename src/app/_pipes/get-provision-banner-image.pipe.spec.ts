import { GetProvisionBannerImagePipe } from './get-provision-banner-image.pipe';

describe('GetProvisionBannerImagePipe', () => {
  it('create an instance', () => {
    const pipe = new GetProvisionBannerImagePipe();
    expect(pipe).toBeTruthy();
  });
});
