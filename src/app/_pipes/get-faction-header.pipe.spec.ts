import { GetFactionHeaderPipe } from './get-faction-header.pipe';

describe('GetFactionHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFactionHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
