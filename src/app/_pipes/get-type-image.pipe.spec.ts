import { GetTypeImagePipe } from './get-type-image.pipe';

describe('GetTypeImagePipe', () => {
  it('create an instance', () => {
    const pipe = new GetTypeImagePipe();
    expect(pipe).toBeTruthy();
  });
});
