import { GetCardbackImagePipe } from './get-cardback-image.pipe';

describe('GetCardbackImagePipe', () => {
  it('create an instance', () => {
    const pipe = new GetCardbackImagePipe();
    expect(pipe).toBeTruthy();
  });
});
