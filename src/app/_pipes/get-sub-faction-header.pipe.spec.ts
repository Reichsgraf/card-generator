import { GetSubFactionHeaderPipe } from './get-sub-faction-header.pipe';

describe('GetSubFactionHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSubFactionHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
