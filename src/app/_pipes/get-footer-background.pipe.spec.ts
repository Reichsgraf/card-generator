import { GetFooterBackgroundPipe } from './get-footer-background.pipe';

describe('GetFooterBackgroundPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFooterBackgroundPipe();
    expect(pipe).toBeTruthy();
  });
});
